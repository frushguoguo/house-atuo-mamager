# 后续落地实施计划（2026-04-29）

## 1. 当前基线
- 已完成：`P0/P1` 最小闭环（`collect -> clean -> analyze -> copywrite`）可运行。
- 已完成：OpenClaw 桥接任务可执行（`task_runner --task full`）。
- 待推进：视频真实生成、多平台真实发布、A+ 生产级闭环、无人值守稳定化。

## 2. 执行原则
- 每个阶段必须满足“验收标准”再进入下一阶段。
- 先单平台/单城市灰度，再扩容。
- 任何自动化能力必须具备日志、重试、告警。

## 3. 分阶段实施清单

## 阶段A：运行依赖与基线校验
- 目标：补齐运行环境，消除基础阻塞。
- 任务：
  - 安装 `ffmpeg`。
  - 安装测试依赖 `pytest`。
  - 固定关键依赖版本并复核 `requirements.txt/pyproject.toml`。
- 验收标准：
  - `python -m property_workflow.orchestration.pipeline --task video --config .\property-workflow-config.yaml` 不再出现 `skipped_ffmpeg_missing`。
  - `python -m pytest` 可执行（至少进入测试流程，不报模块缺失）。
- 执行结果（2026-04-29）：
  - 已安装：`pytest`、`imageio-ffmpeg`。
  - 已完成：视频任务通过 `imageio-ffmpeg` 回退机制成功生成（不再 `skipped_ffmpeg_missing`）。
  - 已完成：依赖清单已更新 `requirements.txt`。
- 状态：`[x]`

## 阶段B：测试体系与回归门禁
- 目标：建立可回归、可持续交付能力。
- 任务：
  - 补 `collect/clean` 单元测试。
  - 补 `analyze/copywrite` 结果结构测试。
  - 补 `pipeline` 端到端冒烟测试。
- 验收标准：
  - `python -m pytest` 全绿。
  - 关键流程失败时能在测试中复现。
- 执行结果（2026-04-29）：
  - 已修复：A+ 桩测试会话方法缺失导致的失败。
  - 已优化：fallback 用例改为快速失败路径，避免超时等待。
  - 当前结果：`python -m pytest -q` -> `9 passed`。
- 状态：`[x]`

## 阶段C：Pipeline 稳定性改造
- 目标：确保任务顺序执行稳定，消除偶发报错。
- 任务：
  - 明确 `publish` 对 `video_generation_report.json` 的前置依赖检查。
  - 为缺失文件、空文件、并发读取增加兜底逻辑。
  - 统一失败退出码与错误信息格式。
- 验收标准：
  - 连续执行 `collect/clean/analyze/copywrite/video/publish` 无随机失败。
  - 失败日志包含任务名、输入、异常原因、建议动作。
- 执行结果（2026-04-29）：
  - 已完成：`publish` 读取 `video_generation_report.json` 时对非法 JSON 自动降级跳过，不再中断任务。
  - 已完成：`video/publish` 对 `copywriting.json` 增加容错读取。
  - 已完成：统一错误输出格式（JSON）与退出码约定，`pipeline` 和 `openclaw-bridge` 共用同一执行入口。
  - 已完成：新增回归测试覆盖配置缺失和输入缺失两类错误。
- 状态：`[x]`

### 阶段C错误码约定
- `E_CONFIG_NOT_FOUND` -> `exit_code=10`
- `E_CONFIG_INVALID` -> `exit_code=11`
- `E_INPUT_MISSING` -> `exit_code=12`
- `E_INVALID_JSON` -> `exit_code=13`
- `E_TASK_UNKNOWN` -> `exit_code=14`
- `E_INTERNAL` -> `exit_code=99`

## 阶段D：视频生成器交付化
- 目标：从“可运行”升级为“可发布素材”。
- 任务：
  - 完成模板化分镜、字幕、BGM、封面流程。
  - 输出真实视频文件与发布元数据。
- 验收标准：
  - 对同一批房源可稳定输出可直接发布的视频。
  - 产物目录包含：成片、字幕、分镜、生成报告。
- 执行结果（2026-04-29）：
  - 已完成：视频生成支持字幕烧录尝试（`captions_path`）。
  - 已完成：字幕烧录失败自动回退为无字幕成片，不中断主流程。
  - 已完成：视频报告新增 `captions_embedded`、`caption_warning` 字段，便于后续质量追踪。
  - 已完成：修复 Windows 字幕滤镜路径转义，当前环境可稳定烧录字幕（`captions_embedded=true`）。
  - 已完成：新增模板视觉滤镜（`default/clean`）与上下信息条样式。
  - 已完成：新增可选 BGM 输入策略（`video_bgm_path` + `video_bgm_volume`）。
  - 待完成：接入真实素材（房源图片/视频片段）替代纯背景底图。
- 状态：`[进行中]`

## 阶段E：多平台发布（先1个平台）
- 目标：打通至少 1 个平台的真实发布闭环。
- 任务：
  - 选定首发平台（抖音/小红书二选一）。
  - 落地鉴权、上传、发布、重试。
  - 增加发布结果回写和失败重投。
- 验收标准：
  - 连续发布成功率达到设定阈值（建议 >= 95%）。
  - 发布报告记录平台侧返回ID与时间戳。
- 状态：`[ ]`

## 阶段F：贝壳A+ 生产闭环
- 目标：从探测脚本升级为可重复执行的业务闭环。
- 任务：
  - 完善登录续期、接口发现、字段映射与冲突处理。
  - 打通抓取数据入库 A+ 与状态回传。
- 验收标准：
  - “抓取 -> A+ 入库 -> 状态同步回传”可重复执行。
  - 关键动作具备操作留痕。
- 状态：`[ ]`

## 阶段G：无人值守调度上线
- 目标：7×24 稳定运行。
- 任务：
  - 使用 `cron-job` / `PM2` 部署定时任务。
  - 配置失败重试与告警通知。
- 验收标准：
  - 连续运行稳定，无人工介入。
  - 异常可自动恢复或自动告警。
- 状态：`[ ]`

## 阶段H：可观测性与审计
- 目标：可追踪、可排障、可审计。
- 任务：
  - 统一结构化日志（任务ID、阶段、输入摘要、输出路径、错误码）。
  - 建立日报/周报汇总（成功率、耗时、失败分布）。
- 验收标准：
  - 任意失败任务可在 5 分钟内定位根因。
  - 审计日志覆盖关键操作链路。
- 状态：`[ ]`

## 阶段I：安全与合规
- 目标：保证数据与账号安全。
- 任务：
  - 敏感配置加密存储、密钥隔离。
  - 最小权限控制与访问审计。
- 验收标准：
  - 密钥不明文落盘。
  - 高风险操作具备可追溯日志。
- 状态：`[ ]`

## 阶段J：灰度与扩容
- 目标：从单点试运行平滑扩到多点。
- 任务：
  - 单城市/单账号/单平台灰度。
  - 达标后扩展到多城市、多账号、多平台。
- 验收标准：
  - 灰度期 KPI 稳定达标后再放量。
  - 扩容后核心成功率无明显下降。
- 状态：`[ ]`

## 4. 每周推进模板
- 本周目标：
- 本周完成：
- 本周阻塞：
- 下周计划：
- 需要外部支持：

## 5. 推荐执行顺序（不跳步）
1. 阶段A
2. 阶段B
3. 阶段C
4. 阶段D
5. 阶段E
6. 阶段F
7. 阶段G
8. 阶段H
9. 阶段I
10. 阶段J
